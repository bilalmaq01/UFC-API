# Local AI/tooling folders
.claude/
.codex/
.agents/

# JetBrains / GoLand
.idea/

# Environment / secrets
.env
.env.*
!.env.example
# Exception: frontend build config holds only the PUBLIC API URL (not a secret)
# and is needed to reproduce the production build, so it's tracked.
!frontend/.env.production

# Go build/test output
bin/
*.test
*.out
coverage.out

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS files
.DS_Store
